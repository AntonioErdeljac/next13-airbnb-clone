import React from 'react';
import ReactDOM from 'react-dom';
import App from './app'; // import the App component from a file

ReactDOM.render(
  <React.StrictMode>
    <app/>
  </React.StrictMode>,
  document.getElementById('root')
);
